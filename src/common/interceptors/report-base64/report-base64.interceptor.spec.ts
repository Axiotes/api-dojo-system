import { ReportBase64Interceptor } from './report-base64.interceptor';

describe('ReportBase64Interceptor', () => {
  it('should be defined', () => {
    expect(new ReportBase64Interceptor()).toBeDefined();
  });
});
