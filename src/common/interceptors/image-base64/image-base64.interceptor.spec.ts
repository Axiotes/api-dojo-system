import { ImageBase64Interceptor } from './image-base64.interceptor';

describe('ImageBase64Interceptor', () => {
  it('should be defined', () => {
    expect(new ImageBase64Interceptor()).toBeDefined();
  });
});
